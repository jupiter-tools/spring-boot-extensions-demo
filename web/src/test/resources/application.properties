
feign.client.config.default.connectTimeout=10000
feign.client.config.default.readTimeout=60000
feign.client.config.default.loggerLevel=full

# Delivery
#delivery-service.ribbon.listOfServers=http://127.0.0.1:${server.port}
#delivery-service.ribbon.eureka.enabled=false

# Templates
#template-service.ribbon.listOfServers=http://127.0.0.1:${server.port}
#template-service.ribbon.eureka.enabled=false
